{
  "name": "shortlink-skipper",
  "version": "1.10.7",
  "private": true,
  "description": "Userscript that skips link shorteners; smoke tests with no external dependencies.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "test:live": "node test-live/run-all.js"
  },
  "devDependencies": {
    "ws": "^8.21.3"
  }
}
